name: Sync LinkedIn Profile Picture

on:
  schedule:
    - cron: "0 0 * * 1"   # Every Monday
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-profile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium --with-deps

      - name: Fetch LinkedIn profile picture
        env:
          LINKEDIN_EMAIL: ${{ secrets.LINKEDIN_EMAIL }}
          LINKEDIN_PASSWORD: ${{ secrets.LINKEDIN_PASSWORD }}
        run: |
          node -e "
          const { chromium } = require('playwright');
          const fs = require('fs');
          const https = require('https');
          const http = require('http');
          const { URL } = require('url');

          function downloadFile(fileUrl, outputPath) {
            return new Promise((resolve, reject) => {
              const urlObj = new URL(fileUrl);
              const protocol = urlObj.protocol === 'https:' ? https : http;

              const file = fs.createWriteStream(outputPath);
              protocol.get(fileUrl, (response) => {
                if (response.statusCode === 301 || response.statusCode === 302) {
                  file.close();
                  downloadFile(response.headers.location, outputPath).then(resolve).catch(reject);
                  return;
                }
                response.pipe(file);
                file.on('finish', () => file.close(resolve));
              }).on('error', (err) => {
                fs.unlink(outputPath, () => {});
                reject(err);
              });
            });
          }

          (async () => {
            const browser = await chromium.launch({
              headless: true,
              args: ['--no-sandbox', '--disable-setuid-sandbox']
            });

            const context = await browser.newContext({
              userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              viewport: { width: 1280, height: 800 }
            });

            const page = await context.newPage();

            try {
              console.log('Navigating to LinkedIn login...');
              await page.goto('https://www.linkedin.com/login', { waitUntil: 'domcontentloaded', timeout: 30000 });
              await page.waitForTimeout(2000);

              console.log('Filling credentials...');
              await page.fill('#username', process.env.LINKEDIN_EMAIL);
              await page.waitForTimeout(500);
              await page.fill('#password', process.env.LINKEDIN_PASSWORD);
              await page.waitForTimeout(500);
              await page.click('[type=submit]');

              console.log('Waiting for login redirect...');
              await page.waitForURL(url => !url.includes('/login'), { timeout: 30000 });
              await page.waitForTimeout(3000);

              console.log('Current URL after login:', page.url());

              // Check for checkpoint/verification
              if (page.url().includes('checkpoint') || page.url().includes('challenge')) {
                console.error('LinkedIn is asking for verification. Try logging in manually once from this IP or use a different account.');
                process.exit(1);
              }

              console.log('Navigating to profile...');
              await page.goto('https://www.linkedin.com/in/karthik-korrayi/', { waitUntil: 'domcontentloaded', timeout: 30000 });
              await page.waitForTimeout(4000);

              console.log('Searching for profile image...');

              // Try multiple selectors LinkedIn uses
              const selectors = [
                'img.pv-top-card-profile-picture__image--show',
                '.pv-top-card__photo img',
                '.profile-photo-edit__preview',
                'img[class*=\"profile-photo\"]',
                'section.artdeco-card img.EntityPhoto-circle',
                'img[class*=\"presence-entity\"]',
                '.pv-top-card--list img',
              ];

              let imgUrl = null;

              for (const selector of selectors) {
                try {
                  const el = await page.$(selector);
                  if (el) {
                    imgUrl = await el.getAttribute('src');
                    if (imgUrl && imgUrl.includes('licdn.com') && !imgUrl.includes('ghost')) {
                      console.log('Found image with selector:', selector);
                      break;
                    }
                  }
                } catch (e) {}
              }

              // Fallback: scan all img tags for licdn profile photo
              if (!imgUrl) {
                console.log('Trying fallback: scanning all images...');
                imgUrl = await page.evaluate(() => {
                  const imgs = Array.from(document.querySelectorAll('img'));
                  const match = imgs.find(img =>
                    img.src &&
                    img.src.includes('licdn.com') &&
                    img.src.includes('profile-displayphoto') &&
                    img.naturalWidth > 100
                  );
                  return match ? match.src : null;
                });
              }

              if (!imgUrl) {
                // Take a screenshot to debug
                await page.screenshot({ path: 'debug-screenshot.png', fullPage: false });
                console.error('Could not find profile image URL. Debug screenshot saved.');
                process.exit(1);
              }

              // Strip query params that might cause issues, keep the base URL
              const cleanUrl = imgUrl.split('?')[0];
              console.log('Downloading image from:', cleanUrl);

              await downloadFile(imgUrl, 'public/images/profile.jpg');

              const stats = fs.statSync('public/images/profile.jpg');
              console.log('Downloaded file size:', stats.size, 'bytes');

              if (stats.size < 5000) {
                console.error('Downloaded file is too small — something went wrong.');
                process.exit(1);
              }

              console.log('Profile picture updated successfully!');

            } catch (err) {
              console.error('Error:', err.message);
              process.exit(1);
            } finally {
              await browser.close();
            }
          })();
          "

      - name: Upload debug screenshot if it exists
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshot
          path: debug-screenshot.png
          if-no-files-found: ignore

      - name: Commit and push
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add public/images/profile.jpg
          git diff --cached --quiet && echo "No changes — profile picture unchanged" || \
          (git commit -m "Auto-update LinkedIn profile picture $(date +%Y-%m-%d)" && git push)
